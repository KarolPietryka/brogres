<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                                http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- 001a: 1× chest + 1× back → dominant "back" (tie-break). That splits the chest block from 005 so /graph only showed 22.03 and 29.03. -->
    <changeSet id="006-drop-dev-mock-2026-03-20" author="brogres" context="dev">
        <sql dbms="h2,postgresql" splitStatements="true" stripComments="true" endDelimiter=";">
            DELETE FROM workout_set
            WHERE workout_id IN (SELECT id FROM workout WHERE workout_date = DATE '2026-03-20');
            DELETE FROM workout WHERE workout_date = DATE '2026-03-20';
        </sql>
    </changeSet>

</databaseChangeLog>
