name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -fsS https://brogres.onrender.com/workout/health || echo "fail"